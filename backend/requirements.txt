# --- Web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# --- Modelos / settings ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6
email-validator>=2.2

# --- Base de datos ---
# Session Pooler de Supabase (IPv4) + asyncpg. greenlet lo exige SQLAlchemy async.
sqlalchemy[asyncio]>=2.0.36,<2.1
asyncpg>=0.30
pgvector>=0.3.6
greenlet>=3.1

# --- IA ---
# google-genai reemplaza a google-generativeai (deprecado). Model ids SIN prefijo "models/".
google-genai>=1.0

# --- Observabilidad ---
langfuse>=3.0
openinference-instrumentation-google-genai

# --- Auth ---
# PyJWT en vez de python-jose (abandonado, CVE-2024-33663 sin fix).
# bcrypt directo en vez de passlib (sin mantenimiento y roto con bcrypt>=4.1).
pyjwt>=2.9
bcrypt>=4.2

# --- Utilidades ---
httpx>=0.27
